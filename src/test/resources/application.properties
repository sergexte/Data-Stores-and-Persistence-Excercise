spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:db
spring.datasource.username=sa
spring.datasource.password=sa

##Remember, the default initialization-mode is embedded, so you do not need to specify this property when using H2 for unit tests.
# Also recall that the default ddl-auto property for in-memory databases is create-drop,
# so you also do not need to set that property unless you wish some other behavior.x